# gitleaks 误报指纹豁免(语法:一行一个 fingerprint,`#` 开头为注释)
#
# 历史误报位置:
#   refs/music-assistant-server/music_assistant/providers/filesystem_cloud/base.py:103
#
# 该行是 Home Assistant setup flow 里的一句错误文案(提示需要填写客户端密钥),
# 属于第三方只读参考实现 —— 不是本仓代码,更不是凭据。gitleaks 的 generic-api-key
# 规则按「客户端密钥关键词 + 相邻引号字符串」命中,熵值仅 3.625(偏低)。
# 该目录已在后续提交里从工作区移出,但 git 历史仍在,故按指纹精确豁免。
#
# 为什么 push 时是绿、只有定时任务红:push 触发的扫描只覆盖本次 push 的提交范围
# (gitleaks-action 会算 --log-opts),schedule / workflow_dispatch 才是全量历史。
#
# 为什么用指纹豁免而不是 .gitleaks.toml:实测加 .gitleaks.toml(即使写了
# [extend] useDefault = true)会改变扫描语义,同一份历史凭空多报一条
# (3rd-party/libflac.js 里的 base64 常量表,熵 6.02)—— 豁免一条冒出另一条。
# 指纹豁免是纯减法:不动规则、不动默认豁免,只屏蔽这一个历史位置;
# 将来新出现的真实凭据 fingerprint 不同,照样会被拦住。
#
# ⚠ 本文件自身也在扫描范围内。切勿在此复写被命中的那句英文原文 ——
#   否则这条注释会被同一条 generic-api-key 规则命中(2026-09-21 已实测踩过一次,
#   当时 fingerprint 为 0748dd9:.gitleaksignore:generic-api-key:10)。
#   下面最后一行是唯一有效内容。
e93716473d708ade584975930d84c56d04094151:refs/music-assistant-server/music_assistant/providers/filesystem_cloud/base.py:generic-api-key:103
