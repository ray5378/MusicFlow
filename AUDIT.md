# MusicFlow UI/UX 审计报告

> 基于 2026-08-06 对部署站点 `http://192.168.10.240:46400/` 的桌面端（1440×900）与移动端（iPhone 14 Pro，390×844）整页截图。

---

## 一、整体印象

应用已完成“飞牛风格”的深色玻璃态重构，主色调、极光背景、玻璃卡片、浮动侧栏、底部播放器都具备较高完成度。当前主要问题集中在：**空状态缺失、移动端适配粗糙、交互反馈不足**。

---

## 二、桌面端问题

### 1. 空内容页大面积留白
**截图**：`desktop-album-detail-full.png`、`desktop-settings-full.png`、`desktop-admin-music-full.png` 等。
**表现**：专辑详情、设置、所有 admin 页面内容极少，剩余区域为纯色/极光背景，用户无法判断是“加载中”还是“没有数据”。
**建议**：统一使用 EmptyState 组件，包含插画/图标、说明文字、操作按钮；为设置页补充账号/主题/缓存等常用设置项。

### 2. 路由切换无过渡
**表现**：点击侧边栏切换页面时，内容直接替换，缺少 fade/slide 过渡，显得生硬。
**建议**：在 `<router-view>` 外套 `<Transition name="page">`，使用 opacity + translateY。

### 3. 卡片 hover 反馈弱
**表现**：卡片仅有轻微上移和阴影变化，缺少 cover zoom、光泽扫过、点击下压的层次感。
**建议**：hover 时 cover 放大 1.06 并叠加 45° 渐变光泽；active 时 scale(0.97)。

### 4. 艺术家页信息重复
**截图**：`desktop-artists.png`。
**表现**：所有艺术家卡片下方几乎统一显示“1张专辑”或“0张专辑”，重复且对信息价值低。
**建议**：0 张专辑不显示数量；有 1 张显示“1 张专辑”；多张显示具体数字；头像 hover 显示播放/收藏按钮。

### 5. 播放列表卡片底部白条风格冲突
**截图**：`desktop-playlists.png`。
**表现**：卡片下方白色信息条与整体深色玻璃风格不一致，且占据较大比例。
**建议**：改为深色渐变底部覆盖层，文字使用白色高对比，保持玻璃卡片一致性。

### 6. 快捷入口图标对齐问题
**截图**：`desktop-home-full.png`。
**表现**：“+ 最近添加”卡片中 `+` 图标视觉重心偏上，与其他两个图标不对齐。
**建议**：统一图标容器大小与垂直居中；使用 line-height 修正加号位置。

### 7. 按钮缺少 tactile press
**表现**：所有按钮点击时没有 scale 反馈，缺少触感。
**建议**：全局按钮 active 时 `transform: scale(0.96)`，配合 100ms ease-out。

---

## 三、移动端问题

### 1. 歌曲列表列被截断
**截图**：`mobile-songs.png`、`mobile-songs-full.png`。
**表现**：标题被截成 “Sl…”，艺术家、专辑、码率、时长列拥挤，表头图标与列错位。
**建议**：移动端隐藏码率/专辑列；标题与艺术家两行显示；时长右对齐；表头仅在桌面显示。

### 2. 专辑/歌单卡片文字溢出
**截图**：`mobile-home-full.png`。
**表现**：“为你迷茫（100首）”等标题折行或被截断，封面文字可读性差。
**建议**：卡片标题限制两行并加省略号；副文本最多一行；调整网格为 2 列并增加行间距。

### 3. 底部播放器遮挡可滚动内容
**截图**：`mobile-home.png`、`mobile-songs.png`。
**表现**：底部固定播放器高度约 70px，内容 scroll 到底时最后一行被遮挡。
**建议**：移动端 `.main-scroll` 增加 `padding-bottom: calc(var(--player-height) + 16px)`。

### 4. Admin 页面在移动端被压成细条
**截图**：`mobile-admin-music-full.png`、`mobile-settings-full.png`。
**表现**：整页截图被拉长后，页面内容被水平压缩到几乎不可见，说明 flex 布局在极端高度下出现挤压。
**建议**：admin 表格/表单在窄屏下使用横向滚动或单列堆叠；避免 flex 子项被异常压缩。

### 5. 移动端侧栏交互缺失确认
**截图**：`mobile-home.png`。
**表现**：仅有汉堡按钮，无法确认抽屉展开后的手势/遮罩/焦点管理。
**建议**：抽屉打开时添加 body 锁定、遮罩层点击关闭、焦点陷阱；关闭按钮明显可见。

---

## 四、优先级

| 优先级 | 事项 |
|--------|------|
| P0 | 修复移动端歌曲列表截断 |
| P0 | 空内容页 EmptyState |
| P0 | 修复移动端底部内容遮挡 |
| P1 | 路由过渡 + 卡片 hover/press 反馈 |
| P1 | 播放列表卡片底部风格统一 |
| P2 | 艺术家标签文案优化 |
| P2 | 快捷入口图标对齐 |

---

## 五、参考截图文件

所有截图位于 `C:\Users\ray5378\WorkBuddy\MusicFlow\.shots\`。

- 桌面视口：`desktop-*.png`
- 桌面整页：`desktop-*-full.png`
- 移动端视口：`mobile-*.png`
- 移动端整页：`mobile-*-full.png`
