# 复制为 .env 并填入随机值(生成: openssl rand -hex 32)
# 用于 JWT 签名与 pass_enc 密码加密,长度必须 >= 32 字符,否则服务拒绝启动
JWT_SECRET=

# 允许跨域直连的 Origin 白名单(逗号分隔),设为 * 则允许全部
CORS_ORIGINS=http://localhost:46399,http://127.0.0.1:46399

# 播放历史保留天数
PLAY_HISTORY_RETENTION_DAYS=180
