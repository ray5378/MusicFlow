# 所有播放链路跳转进度优化方案

> **建立日期：2026-09-22**（真机数据同日在 240 生产实例采集：天翼网盘 WebDAV 曲库 × ESP32 `esp32-player2`
> `C4:9E:7E:08:75:64`；四条链路调研于同日 22:30~22:45 补入）
>
> **本文是「所有播放链路 · 跳转进度（seek）性能优化」的单一真相源**，覆盖五条链路：
> **Sendspin**（服务端推流给 ESP32 音箱）、**DLNA**（服务端投屏 + 设备拉流）、
> **客户端自己播放**（Flutter/安卓/Windows）、**Web 播放**、**AirPlay**。
> seek 的**语义与正确性**（世代竞态、钳制、源行复用契约）仍归 `docs/PLAYBACK_SEEK_MA_REWORK.md`；
> 本文只管**性能**：拖一次进度条要多久才出声、以及怎么把它降下来。

## 0. 一句话

用户报「很多歌曲跳转进度后要十几秒才出声」。定位到**三段互相独立的开销**：

| 段 | 内容 | 量级 | 影响链路 | 状态 |
|---|---|---|---|---|
| ① | 每次 seek 重建都重跑一遍「播放优选」（`resolvePlayableRow`） | **1.85~2.53s** | Sendspin | ✅ **已修（A 项 `1bcd473`）** |
| ② | ffmpeg 对**无 SEEKTABLE** 的网盘 FLAC 做输入 `-ss` 时发 9 次**开放式 Range**，每次一个上游往返 | **4~13s** | **全部五条** | ✅ **已落地并验收**（B 项 §6，2026-09-23） |
| ③ | 四条链路每次 seek 也重跑 `resolvePreferredSong` → 真的去 HEAD 探测 WebDAV 候选 | **1.0~2.9s** | DLNA / 客户端 / Web / AirPlay | ⬜ 未开工（F 项 §7） |

**②才是大头**，且它是**五条链路共有的**。本文把六项候选（A~F）的取舍、**优化方法**、实测数据与状态固化下来。

> ⚠️ **落地范围先看清**：① 的 A 项**只在 Sendspin 链路的拖进度上生效**；
> **DLNA / 客户端自己播放 / Web 播放 / AirPlay 这四条链路的优化，目前一项都还没做**
> （A 形态对它们不适用，要等 F；B/C/D 也未落地）。逐链路状态见 **§4.1**。
>
> 用户口径原话（2026-09-22）：
> > A 的优化在其他链路还没做 —— 应该说其他链路的优化都没做。

## 1. 症状与用户口径

- **症状**：拖动进度条后 10 几秒才出声。位置**发布是对的**（UI 上进度条立刻到目标值），
  但音频迟迟不来；期间设备侧无报错，服务端只在等音源。
- **用户口径（原话）**：
  > 网络源 / WebDAV 源跳转进度时应该**自动复用正在播放的地址**，不应该回退到「查找播放源」这一步。

  这一条直接催生了 A 项（源行复用）。
- **补充口径（2026-09-22 晚）**：
  > 服务端推 DLNA、客户端自己播放、Web 播放、AirPlay 播放的链路，在跳转进度时你看看能不能也用 A 这个优化加速。

  → 由此补出 §7（F 项）。

## 2. 真机实测（240 生产实例，2026-09-22）

### 2.1 逐层排除（每一步的初判都被实测推翻过一次）

1. **网络 / Range → 排除**：源站 web 源 206、整首 4.29MB / 1.03s；WebDAV 任意位置 Range 均 206 / ~271ms。
2. **ffmpeg / loudnorm → 排除（但先踩了一个假数据的坑）**：起初直连 WebDAV `-ss 67` 测出「258ms」——
   那是**假数据**：`-headers` 里的 `\r\n` 经 shell 截断成换行 ⇒ ffmpeg 报 `No trailing CRLF found` +
   `Error opening input`，**根本没打开输入**。改用 `execFileSync(FF, [args...])` 数组传参复测，
   loudnorm 各变体均 200~300ms。
3. **复现链路（独立脚本逐步计时）**：`resolvePlayableRow` = **1925ms**（`preferred-swap`），
   其余 `resolveRowInput` / mint token / `isStreamSource` 全 ≤2ms。
4. **回环 token URL（生产真实输入）**：`-ss 67`=**4493ms**、`-ss 129`=**14344ms**、`-ss 178`=**3741ms**
   —— 与生产日志逐个吻合（6.84 / 15.34 / 7.03s）。直连 vs 回环**单请求耗时相近**（~340 vs ~370ms）
   ⇒ **回环转发不是主因**。
5. **`ffmpeg -v debug` 抓机制**：**9 次 `Range: bytes=N-`（全部开放式）**，后半截在几百字节内逐帧回溯同步：
   `7923532- → 7360604- → 7294107- → 7287723- → 7288901-`。

### 2.2 端到端口径补测（同日夜间，带 `-t` 时间戳）

| 口径 | 实测 |
|---|---|
| seek 重建「开始获取音源 → 音源就绪」 | **5.1 / 5.1 / 5.8 / 9.4 / 13.1s** |
| 首播同口径 · v4.0.12 原版 | 2.5 / 4.6 / 3.3s |
| 首播同口径 · 补丁版（A + 竞态修复） | 0.65 / 1.46 / 2.67s ⇒ **无回归** |
| A 项生效证据 | 同曲 seek `(reuse-active) ms=1~15`（原 `preferred-swap` 1850~2530ms） |

> ⚠️ **跨版本比耗时必须在切换版本前落盘**：`docker restart` 会截断 `docker logs`（只留到最近一次重启），
> 这次先切了版本，v4.0.12 的 **seek** 耗时样本一并丢失，只能拿首播做 A/B。

## 3. 机理

- **曲库形态**：本实例曲库**全是天翼网盘 WebDAV**（`w:<sourceId>:` 前缀，31MB FLAC，**无 SEEKTABLE**）。
- **FLAC 无 SEEKTABLE** ⇒ demuxer 拿不到帧索引，只能按码率**估算**目标字节位置，`avio_seek` 过去、
  扫帧头校验，不中则回溯重试；**每一次 `avio_seek` 在 HTTP 输入上就是一次新的带 `Range: bytes=N-` 的请求**。
- **链路**：`PcmWindow.spawn()` → `decodeArgs({ timeOffsetSec })`（`backend/src/services/audio/pipeline.ts:66`）
  → `-ss <秒>` 落在 `-i` **之前** → ffmpeg 走「输入 seek」。
- **输入是回环 token URL**（`/rest/dlna/stream/<token>?raw=1`，SPEC §1.8 硬契约），
  而该路由是**纯透传代理**（`routes/rest/index.ts` 的 `restRoutes.get("/dlna/stream/:token")` raw 分支：
  `fetch(upstream, { Range })` → `c.body(upstream.body)`）
  ⇒ **ffmpeg 的每一次 Range 都是一次完整的上游往返（~340ms）**。
  9 次 ≈ 3s 起步，叠加传输与回溯，落到 4~13s。
- **位置相关性**：`-ss` 越大，估算点越远、回溯链越长 ⇒ 通常越慢（但受上游波动影响，非严格单调：
  实测 `-ss 129`=14.3s 而 `-ss 178`=3.7s）。
- **①的机理（A 项要治的那段）**：`PumpSource(songId, startMs)` **只带 songId、不带「当前正在用的行」**，
  于是同一首歌内反复拖进度条也每次重解析 —— 快缓存 `getCachedPlayability` 只记「可播/不可播」、
  **不记换到了哪一行**；而本实例每条 web 歌都会**恒定**换到组内「核心曲库」行（天翼网盘行），
  即「结果恒定却每次重算」。
- **③的机理（F 项要治的那段）**：见 §7.2 —— 与①同源（结果恒定却每次重算），但发生在**另外四条链路**上，
  且那四条是**无状态 HTTP 重拉**、调用方没有「上一次用了哪一行」的记忆。

## 4. 候选清单（A~F）

| 项 | 做法 | 预期收益 | 风险 / 代价 | 状态 |
|---|---|---|---|---|
| **A** | **复用当前生效源行**（详见 §5） | 省 1.85~2.53s | 复用命中却出不了流 → 清记账 + 回退完整裁决一次 | ✅ **已完成** `1bcd473` |
| **B** | **让 ffmpeg 拿到可随机访问的输入**（回环节点 256KB 稀疏块缓存；可选本地文件路径）（详见 §6） | 省 4~13s（**收益最大**） | 内存/磁盘定额、缓存键必须含 url 指纹、异常必须回退透传、**首响应不得等数据**（§6.6） | ✅ **已落地并验收**（2026-09-23；离线 24 单测 + 11 守卫，240 四轮实测 + 用户收听验收通过） |
| **C** | **窗口内 seek 走零成本路径**：`PcmWindow.seekTo()` 已有命中窗口的快速分支（只挪消费水位），但 `seekCore` 总走「完整起播重建」⇒ 每次拖动都重起一条 ffmpeg | 省一次 ffmpeg spawn + 预缓冲 | 需与「起播窗口内 seek 世代竞态」（`1937f80`）的语义对齐，别把已修好的竞态重新引回来 | ⬜ 未开工 |
| **D** | **前端拖拽预热**：拖动过程中（`onChange` 节流）预取目标附近字节，落热到 B 的缓存 | 让 B 的首个 miss 也提前发生 | 与现有 250ms trailing 去抖的关系要理清；别把请求放大 | ⬜ 未开工 |
| **E** | **`-noaccurate_seek` 输入粗定位**（`-ss` 前追加，须在 `-i` 前） | 孤立 ffmpeg 上量到 **-29%**（请求数不变） | 真实 seek 重建路径上**无法确认收益**；同期出现的取流超时经取证另有根因（起播窗口内 seek 世代竞态）。**按用户决定摘除**，原文保留在 §11.1 | ⛔ **已撤回** |
| **F** | **「WebDAV 可播」结论做成短 TTL 记忆**（详见 §7） | 四条链路每次 seek 省 **1.0~2.9s** | 只缓存 webdav 分支（本地行仍实时 `existsSync`）、TTL 与失败记忆对称、出流失败逐出 | ⬜ **未开工** |

### 4.1 落地状态总表（按链路 × 优化项）

> ⚠️ **一句话：除 Sendspin 之外，其余四条链路的优化，一项都还没做。**
> 而且那四条链路**连 A 的形态都用不上** —— A 要求「调用方记账上一轮用了哪一行」，
> 而它们是无状态 HTTP 重拉、没有可记账的调用方（原因见 §7.1）。它们要等的是 **F**，
> 而 **F 与 B / C / D 也都还没落地**。

| 链路 | ① 重跑播放优选 | ② 输入 `-ss` 上游往返 | C 窗口内 seek | D 拖拽预热 |
|---|---|---|---|---|
| **Sendspin**（服务端推流给 ESP32） | ✅ **A 已做**（`1bcd473`） | ✅ **B 已落地并验收**（2026-09-23） | ⬜ 未做 | ⬜ 未做 |
| **DLNA**（服务端投屏 + 设备拉流） | ⬜ **未做**（A 不适用，待 F） | ⬜ 未做 | ⬜ 未做 | ⬜ 未做 |
| **Web 播放** | ⬜ **未做**（同上） | ⬜ 未做 | ⬜ 未做 | ⬜ 未做 |
| **客户端自己播放**（Flutter/安卓/Windows） | ⬜ **未做**（同上） | ⬜ 未做 | ⬜ 未做 | ⬜ 未做 |
| **AirPlay** | ⬜ **未做**（同上） | ⬜ 未做 | ⬜ 未做 | ⬜ 未做 |

**读表要点**：

1. **A 的代码路径只覆盖 Sendspin 的拖进度**。`resolvePlayableRow(songId, { preferRowId })` 目前只有两个调用方：
   sendspin 的 `GroupPump`，以及 `QueueController.judgePlayable:735` —— 后者是**切歌判定**、不是拖进度，
   所以**不构成**上表任何一行的 ✅。
2. **② 是五条链路共用的一段**（`audio/pipeline.ts` 的 `decodeArgs`）⇒ **B 一旦落地，五条链路同时受益**。
   这也是把 B 排在 F 前面的原因：收益更大（4~13s vs 1~3s）、覆盖面更广。
3. 表里的「未做」= 代码里**没有任何**针对该链路的 seek 性能改动，**不是**「做了但没生效」。

**取舍结论**：A 已解决①（✅）；**B 解决②**（收益最大，且**五条链路通吃**）；
**F 解决③**（四条链路的同类开销，改动最小）。C/D 作为后续候选保留；E 不再单独做（它治的是
「请求数不变、每请求更快」，与 B 的「请求数归并」正交 —— B 落地后 E 的更小收益更不值得）。

## 5. A 项 · 复用当前生效源行【✅ 已完成 · `1bcd473`】

> commit：`1bcd473 perf(seek): seek 重建复用生效源行,省掉每次 1.85~2.53s 的播放优选`
>
> ⚠️ **适用范围**：本项只覆盖**主动维护记账的调用方** —— sendspin pump 与 `QueueController.judgePlayable`。
> 另外四条链路**不适用**（它们是无状态 HTTP 重拉，见 §7.1），那部分由 **F 项**覆盖。

### 5.1 要治什么

`resolvePlayableRow(songId)` 每次调用都重跑一遍「播放优选」：web 行先 `resolvePreferredSong` 换行、
再 `ensurePlayableStream`、再 `verifyRow` 逐候选探测 —— 实测 **1.85~2.53s/次**，
而本实例的结果**恒定**（总能换到组内「核心曲库」的天翼网盘行）。

根因是**上下文没传下来**：seek 重建走 `PumpSource(songId, startMs)`，**只带 songId、不带「当前正在用的行」**；
快缓存 `getCachedPlayability` 只记 playable、**不记换到了哪一行**。于是同一首歌内每次拖动都白跑一遍。

### 5.2 优化方法（4 个文件）

| 文件 | 改动 |
|---|---|
| `services/source/resolveAudio.ts` | `resolvePlayableRow(songId, opts?)` 新增 `opts.preferRowId` —— **命中即零成本直返**（`reason=reuse-active`），整段优选跳过。判定**放在快缓存之前**（关键：快缓存回的永远是**原始 songId** 那一行，而实际在播的可能是优选换过的**组内兄弟行**） |
| `services/sendspin/streamEngine.ts` | ① `GroupAudio` 增加 `sourceRowId`（回带当前生效行）；② `PumpSource` 加第三参 `opts`；③ `GroupPump` 记 `{ songId, rowId }`，**仅同曲** seek 重建时传回 |
| 同上 | 复用命中却出不了流 → **清记账 + 回退完整裁决一次**（回退成功按新结果**重建记账**，不会永久退化） |
| 同上 | **切歌不复用**（songId 不一致直接丢弃记账） |

设计依据就是用户口径：「网络源 / WebDAV 源跳转进度时应该自动复用正在播放的地址，不应该回退到查找播放源这一步」。

### 5.3 实测收益

| 指标 | 改前 | 改后 |
|---|---|---|
| 同曲 seek 重建的解析耗时 | `preferred-swap` **1850~2530ms** | `(reuse-active)` **1~15ms** |
| 切歌 | `preferred-swap` ~2s | ~1.9~2.1s（**设计如此**：切歌不复用） |

240 热补丁实测日志：`复用源行=<rowId>` + `[resolve] <A> -> <A> (reuse-active) ms=2`。

### 5.4 测试与负向验证

新增/补充 **8 个用例**：

- `tests/services/resolveAudio.test.ts`（4）：命中直返 / 命中兄弟行 / 行不存在回退 / 不传不变
- `src/services/sendspin/streamPumpRowReuse.test.ts`（4，新增）：同曲复用 / 切歌不复用 /
  失败回退一次并重建记账 / 无 `sourceRowId` 不复用

**负向验证矩阵（5 变体，每个精确变红，失败信息恰好点中所修语义）**：

| 变体 | 破坏点 | 红了几例 |
|---|---|---|
| A | 去掉 resolveAudio 的复用分支 | 2（两条「命中」用例） |
| B | streamEngine 不透传 `preferRowId` | 2（同曲复用 / 失败回退） |
| C | 去掉 `-noaccurate_seek` 追加 | 1（有 `-ss` 才加） |
| C2 | 改成「只要带 `-ss` 就无条件加」 | 1（不传则保持精确） |
| C3 | 把粗定位提到 `-ss` 分支之外 | 1（无 `-ss` 不加） |

> 经验（已并入 `MEMORY.md`）：**一个修复含「方向相反」的守卫时，要分别造「移除」与「过度开启」两类变体** ——
> 只破坏一处的话，另一条断言可能从没红过。

230 验证：`tsc --noEmit` 通过；受影响 3 文件 **37 用例全绿**。

### 5.5 文档回标

`SPEC.md` §1.7（`preferRowId` / `reuse-active`）+ §1.8 新增「输入 seek 的粒度契约」；
`docs/PLAYBACK_SEEK_MA_REWORK.md` 状态行 1~7→1~8、§5 新增 **patch9**、修订记录 **R3**。

## 6. B 项 · 让 ffmpeg 拿到可随机访问的输入【✅ 已落地并验收 2026-09-23】

> 代码：`backend/src/services/dlna/rawStreamCache.ts`（新增）+ `backend/src/routes/rest/index.ts`
> 的 `/dlna/stream/:token?raw=1` 分支（由纯透传改为调用 `proxyRawRange`）。
> **离线闭环**：24 单测 + 11 条负向守卫（各精确变红，V1b 对照组全绿）+ 全量回归 1563 例 + 9 门禁全 0。
> **240 真机**：四轮实测（前三轮翻车均即时回滚并定位根因，B5 通过全部探针，见 §6.8），
> **用户收听验收已通过**（起播/跳转后立刻有声、跳转后不再卡住，两个症状面均已消除）。
> 发版状态见 §6.9。

### 6.1 硬约束

- **SPEC §1.8**：ffmpeg 子进程的输入**只允许两类** —— 回环 token URL（`/rest/dlna/stream/<token>?raw=1`）
  或**本地文件路径**。B 的两个落点都在契约内，**不新增第三类输入**。
- 不改「取源」语义：仍由 Node 侧做鉴权 / 302 / 播放优选；**不恢复任何直出旁路**。
- 上游请求数要**降**（不是把 9 次小请求变成 9 次大请求）。
- **首响应不允许等数据**（2026-09-23 事故沉淀，详见 §6.6）。

### 6.2 实际落地形态（与原型方案的出入）

1. **回环节点稀疏块缓存**（主方案，对 ffmpeg 透明）
   把 `/rest/dlna/stream/:token?raw=1` 从「纯透传」升级为「**256KB 稀疏块缓存代理**」：
   - 未命中 ⇒ **透传 + 镜像**（转发上游状态行与头，pull 时把经过的字节写入缓存，受单次镜像预算约束）；
   - 命中 ⇒ **本地供给，零上游往返**；缓存用尽的部分转回上游续接（仍边下边喂）；
   - 每次未命中**后台补一整块** `[块首, 块尾)`：ffmpeg 的回溯是逐步**往前**退的
     （实测 `15188724 → 15111738 → 15110563 → 15109290 → 15108053`），不补块则更早的 Range 永远命中不了；
     ⚠️ 补块**不带客户端 `signal`**（带了必然被自己掐断，见 §6.6 第 1 条）；
   - 缓存键 = **url + 鉴权头**（web 行换源后 url 会变，不能只按 rowId/歌名），LRU 定额。

   **与原型方案的三处出入（别照原型找代码）**：

   | 原型方案（§6.3.1 结论） | 实际落地 | 为什么改 |
   |---|---|---|
   | 缓存落 `DATA_DIR/cache/raw/`（磁盘） | **进程内内存**：单源 16MB / 最多 4 源 / 单次镜像 2MB | seek 回溯只需要目标点附近 **~85KB**，磁盘带来的 IO、清理、跨进程一致性成本不划算；且「开始占用用户磁盘」是**产品口径变化**，不该在一次性能优化里顺手引入 |
   | seek 前按「目标时间 → 估算字节位置」预热 | **不做预热**，改由上面的**并行补前缀**覆盖 | 补前缀用的是 ffmpeg **真实来过的**位置，比按 `total × t / duration` 估算更准（估算误差本来也只能靠 ffmpeg 自己的回溯吸收） |
   | 响应必须**有界**（单次最多回一块） | **沿用上游响应形态**（不做自造的有界响应） | 自造有界响应会让「首响应必须等到一个块下完」，直接触发 §6.6 的事故；无界是旧版透传的既有性质，ffmpeg 本来就是读一点就断连 |

2. **本地文件路径输入**（可选加速，**未做**）
   当整曲已在本地缓存（如用户反复拖动同一首）时，`resolvePipelineInput` 直接回**本地路径**，
   `-ss` 完全无网络、只有本地读 + 帧扫描。本轮**没有做**这一条 —— 它要求「整曲已落本地」，
   与上面「缓存只放进程内内存、不落盘」的决定冲突，留待后续单独评估。

### 6.3 收益实测（2026-09-22 · 240 容器内 · `patch/probe_B_240.js`）

同一首歌（31,450,208 字节 FLAC，天翼网盘 WebDAV），只换**输入形态**：

| 输入形态 | `-ss 67` | `-ss 129` | `-ss 178` |
|---|---|---|---|
| **现状：回环 token URL 透传** | **4708ms** | **8862ms** | **3369ms** |
| **本地文件路径（收益上界）** | **13ms** | **13ms** | **10ms** |

- ⇒ **收益上界 ≈ 8.9s → 13ms（约 680×）**。本地输入下 `-ss` 几乎零成本，证实「慢的只有网络往返」。
- 旁证：同一时刻「单次上游 Range」= **753ms**（HTTP 206），整曲 31.4MB 下载 = **1793ms**
  ⇒ 现状那 4.7/8.9/3.4s 正是若干次这样的往返叠加。
- **不采用「先下整曲再喂本地文件」**：31MB 要 1.8s、大文件更久，且白拉用户不会听的部分。

#### 6.3.1 主方案原型验证（`patch/probe_C3_240.js`：本地 HTTP + 4MB 块预读）

把输入换成「本地 HTTP 端点 + 按块预读缓存」（= B 主方案的原型）：

| 场景 | 耗时 | 上游请求 | 说明 |
|---|---|---|---|
| 冷缓存 `-ss 129`（首拖到新位置） | **3875ms** | 6 | ffmpeg 的 seek 序列散落在 6 个 4MB 块上 ⇒ 6 次回源 |
| **热缓存 `-ss 129`**（紧接着再拖） | **72ms** | **0** | 全命中 ⇒ 只剩本地读 |
| **热缓存 `-ss 67`** | **28ms** | **0** | 同上 |
| 热缓存 `-ss 178`（该处块未缓存） | 1553ms | 2 | 只补 2 块 |
| `-ss 250`（块已被 open 阶段带入） | **23ms** | 0 | — |

**结论**：① 方案成立 —— 热缓存下 **23~72ms**（vs 现状 **8862ms**）；
② 冷态仍有 ~3.9s，根因是 **4MB 块太大**：实测 ffmpeg 的 seek 回溯序列集中在目标点
**前后 ~85KB** 内（`15188724 → 15111738 → 15110563 → 15109290 → 15108053 …`），
真正需要的是**小块 + 目标点预热**，不是大块。
⇒ 实现定为：**小块（256KB 量级）+ seek 前按「目标时间 → 估算字节位置」预热该窗口**
（时间→字节用 `total × t / duration` 估算；估算误差由 ffmpeg 的回溯窗口吸收）。

> ⚠️ **原型三次踩坑（都记下来，实现时别再犯）**
> 1. **v1（`probe_B_240.js` 的 C 段）：开放式 Range 无界响应 ⇒ 死循环**。
>    对 `bytes=N-` 逐块 `await` 直到文件末尾（20MB+），且 `take<=0` 无保护 ⇒ 空转卡满 11 分钟。
>    **教训：「可随机访问的输入」必须「单次响应有界」**，否则 seek 退化成整曲下载。
> 2. **v1/v2 共同的真凶：`spawnSync` + 同进程 HTTP 服务 = 事件循环死锁**。
>    `spawnSync` 阻塞整个 Node 事件循环 ⇒ 同进程的缓存服务器**无法应答** ffmpeg 的请求 ⇒ 挂到超时。
>    指纹：**日志里 `上游请求=0` 且 handler 一条日志都没打**（v1 无 timeout 故卡 11 分钟；
>    v2 卡到 90s `ETIMEDOUT`）。改异步 `spawn`（v3）后立刻正常。
>    ⇒ **实现时缓存服务与 ffmpeg 必须能共用事件循环**（同进程异步，或分开进程）。
> 3. **块大小不能拍脑袋**：4MB 块使冷态仍需 6 次回源（≈3.9s）；应取「覆盖回溯窗口」的小块。

### 6.4 验收条件与结果

**离线（每次改动必跑，出红即不可发版）**：

| # | 验收条件 | 怎么取 | 通过标准 | 本轮 |
|---|---|---|---|---|
| O1 | 有界 Range 四种语义**逐字节等价** | `tests/services/rawStreamCache.test.ts` | 200 / 有界 / 开放式 / suffix 全对；越界回 **416** | ✅ 24 例全绿 |
| O2 | 首响应**不等数据** | 上游 `hold` 形态（只发头、body 卡住） | 3s 内返回 206 且头齐全 | ✅ |
| O3 | 缓存用尽后续接**只回源一次** | 计数上游收到的请求数 | 续接期间上游请求数 **= 1**（不是每 chunk 1 次）| ✅ |
| O4 | 续接交给上游后**不重复吐字节** | 整曲回环 vs 直连逐字节比对 | 回环两遍的 md5 与直连全等 | ✅ |
| O5 | 异常路径**回退透传**、不得 5xx | 上游 5xx / 不认 Range / `RAW_STREAM_CACHE=0` | 回退透传不 5xx；5xx 先重试 1 次、仍失败要让下游看到**错误而非假 EOF** | ✅ |
| O6 | **守卫真的会红** | `neg_rawcache.js` 12 变体 | 11 条守卫各自**精确变红** + V1b 对照组全绿 | ✅ |
| O7 | 无回归 | `tsc --noEmit` + 全量 vitest + 9 个 CI 门禁 | 全绿 / 全 0 | ✅ 184 文件 1563 用例 |

**真机（240，发版前必做；前三轮都在这里翻车）**：

| # | 验收条件 | 怎么取 | 通过标准 | 本轮 |
|---|---|---|---|---|
| R1 | **起播 / 跳转后立刻有声**（不得有前奏几秒静音） | 用户收听 | 感知上立即出声 | ✅ 通过 |
| R2 | **跳转后不卡住**（不得播一会停住） | 用户收听 + `probe_B3c_240.js`（真 ffmpeg `-ss`） | 冷态 **≤2s**；不出现「播一会停住」/ 21 分钟零输出 | ✅ 冷 1.3~1.9s · 热 31ms |
| R3 | 字节与直连**完全一致** | `probe_B3d_240.js`（md5 三方比对） | 直连×2 与回环×2 **全等** | ✅ |
| R4 | 整曲**不截断、不退化** | `probe_B3_240.js` | `len` 与直连一致；耗时 ≤ 直连 × 1.5 | ✅ 3.5s vs 3.7s |
| R5 | 上游 5xx **不得静默收尾** | `docker logs musicflow \| grep -c "续接彻底失败"` | **= 0**（重试后成功）；有告警但不是假 EOF | ✅ 0 次 |
| R6 | 命中分支不丢响应头 | `probe_cl_240.js`（裸 TCP） | 确实发出 `content-length` / `content-type` 与上游一致 | ✅ |

> **不列为验收条件（明确不做）**：「回溯序列 9 → 1~2」。真机 5 次回溯只命中 1 次 —— 补块落地
> TTFB ≈450ms 与 ffmpeg 回溯请求间隔**同一量级**，要「第一次回溯就命中」只能等数据 ⇒ 撞 §6.6 首响应红线。
> ⇒ 收益口径改为**整曲续读零回源 + 热态重放**（31ms vs 基线 8862ms），见 §6.8「已知差距」。

**下一次复验入口**（交接用）：离线 `npx vitest run tests/services/rawStreamCache.test.ts` +
`node neg_rawcache.js restore`；真机 `patch/probe_B3_240.js`、`probe_B3c_240.js`、`probe_B3d_240.js`。

### 6.5 覆盖链路（重要）

`audio/pipeline.ts:72-73`：`timeOffsetSec > 0` ⇒ `args.push("-ss", String(timeOffsetSec))`，落在 `-i` **之前**。
**五条链路的 seek 都走这一段**（Sendspin 的 `PcmWindow.spawn` 与 `/rest/stream`、`serveCastStream` 的
`servePipelinedSong` 共用 `buildPipelineCommand`）⇒ **B 的收益不是 sendspin 专属，全部五条链路通吃**。

### 6.6 形态红线：首响应**不允许**等数据（2026-09-23 事故沉淀）

第一版实现是「先把 256KB 窗口整段下载完，再构造响应」。上 240 真机后用户报
「**前面几秒没有声音，拖动后声音就正常了**」→ 回滚；第二版改进后仍报
「**跳转后一直卡住**」→ 再次回滚。两次的**共同根因**：

> 旧版纯透传之所以不出事，是因为它**拿到上游响应头就立刻转发** —— 首响延迟只等一个 TTFB。
> 一旦在回话前加了一次「等数据」，上游（网盘）并发一紧张就把首响应拖到几十秒
> ⇒ 服务端 **408**、ffmpeg 卡住、听感退化成「拖动后一直卡住 / 没声音」。

⇒ 本层的**硬性性质**：**响应头在上游响应头到达后立刻发出，数据边下边喂**。
对应到代码：`openUpstream()` **只 `await` 响应头，绝不 `await arrayBuffer()`**；
`proxyRawRange()` 未命中分支用 `openUpstream()` 的响应直接构造流式 `Response`。

**同一轮里一并沉淀的四条坑（都是离线测试**测不出来**、只有真机形态会暴露的）**：

1. **后台补块绝不能带客户端 `signal`**。ffmpeg 的回溯是「读几百字节就断连、再发下一个 Range」，
   请求一结束 signal 就 abort ⇒ 带着 signal 的补块**必然被自己掐断**，缓存永远补不上，
   **优化在真机上等于白做**（开关开着却没有收益）。主线响应仍带 signal（客户端跑了就该停），
   **只有后台补块不带**。⚠️ 单测天然覆盖不到：测试直接调 `proxyRawRange`，压根不传 signal。
2. **短读必须续抓**：上游按自己的粒度分片给（短读）时，原地反复重抓同一段会让流**被静默截断**
   （症状：前奏无声 / 播到一半断）；本层改为按游标循环续接。
3. **后台补块必须 `.catch()`**：后台 `fetch` 失败若冒泡成 unhandled rejection，
   **Node 默认会直接杀进程** —— 这是「优化项把整个服务搞挂」的最短路径。
4. **块内的空洞不能被合并掉**：Block 只有一个 `[from,to)` 区间，**不相连的两段必须丢弃旧的、只认新写的**
   否则中间的空洞会被算成有效数据 ⇒ 给 ffmpeg 喂零字节 ⇒ 「拖动后无声」。
   典型来源就是上游短读 + 补块部分失败。
5. **续接交给上游之后，绝不再回头读缓存**。上游那条流是顺序覆盖 `[tailStart, EOF]` 的，
   若中途又命中一个已缓存的块，那段字节会被**吐两次** ⇒ 整条流「前面多一段、末尾被
   `pos > lastByte` 截掉抵消」，**总长度还一样**，md5 才看得出来。
   240 实测：整曲回环 md5 与直连不符、两次回环互不相同，首个差异正好落在块边界（786432 = 3×256KB）。
   取证手法：`probe_B3d_240.js`（直连读两遍 + 回环读两遍比 md5）+ `probe_B3f_240.js`
   （在回环里搜直连的 32B 窗口 ⇒ 偏移差不为 0 即「错位」而非「乱码」）。
6. **续接失败必须报错，不能静默收尾**。上游偶发 5xx（实测网盘会返回 500）时若 `return null`
   收尾，下游会当成「这首歌播完了」⇒ sendspin 自动切下一首，**比一个明确的取流错误更糟**。
   现在是「重试一次 + 仍失败则 throw」。
7. **命中后「续接」必须一条上游请求读到完**（本轮最严重的真机事故）。
   写成「缓存用尽时现场 `openUpstream` 读一个 chunk」⇒ 缓存（256KB ≈ **2 秒音频**）放完之后
   变成**每个 chunk 一次网盘往返**（实测 TTFB ≈450ms）⇒ 听感正是用户报的
   「**跳转后播放正常一会，然后停住**」。240 实测同一首歌 `ffmpeg -t 2`：
   正常 **2.2s** → 事故形态 **21 分钟且零输出**（`patch/probe_B3c_240.js`）。
   修法：续接的 reader 提到闭包里复用，`if (!tailReader) openUpstream(...)`，之后一直读到 `done`。

### 6.7 测试与负向验证（离线，2026-09-23）

新增 `backend/tests/services/rawStreamCache.test.ts` —— **24 例**，上游是**真 HTTP 服务**
（支持分片慢给、只发头不发 body 的 `hold` 形态、整块请求一律 416 的「补块失败」形态、
可注入 5xx 的「上游抖动」形态），覆盖：
Range 四种语义逐字节等价、上游分片慢给仍完整交付、**首响应不等数据**、块空洞不喂零、
不相连区间不被合并、命中零往返、**回溯序列只回源 2 次**、续接只回源一次、
续接遇 5xx 重试一次后仍完整交付、降级（上游不认 Range / `RAW_STREAM_CACHE=0`）、
内存预算与镜像限量、不同鉴权不共享条目。

> ⚠️ 「回溯序列只回源 2 次」是**单测口径**（上游是本地真 HTTP，TTFB≈0 ⇒ 补块瞬间落地）。
> **真机口径完全不是这个数**，见 §6.8 —— 别把单测数字当收益。

**负向验证矩阵（12 变体：11 条守卫 + 1 组对照，全部在 230 实跑）**：

| 变体 | 破坏点 | 结果 |
|---|---|---|
| V0 | 首响应等一片数据（事故原形态） | 🔴 5 例红 |
| V1 | 补块不做块对齐 ⇒ 块永远补不上 | 🔴 3 例红 |
| **V1b** | **对照组**：去掉「块已满就不补」的提前返回 | 🟢 **24 全绿** |
| V2 | 不做缓存查找 ⇒ 退化成旧行为 | 🔴 5 例红 |
| V3 | 响应从块头开始而不是请求起点 ⇒ 整首错位 | 🔴 5 例红 |
| V4 | 忽略块的有效区间 ⇒ 给 ffmpeg 喂零字节 | 🔴 3 例红 |
| V5 | 命中供给按块长度取（越界吐零） | 🔴 1 例红 |
| V6 | 不相连的两段强行合并 ⇒ 空洞被当数据 | 🔴 1 例红 |
| V7 | 续接改成每 chunk 新开上游请求（21 分钟事故） | 🔴 1 例红 |
| V8 | 续接交给上游后仍回头读缓存 ⇒ 同一段吐两次 | 🔴 1 例红 |
| V9 | 续接遇 5xx 静默 `return null` ⇒ 假 EOF 被当成播完 | 🔴 1 例红 |
| V10 | 不重试、一次 5xx 就整段放弃 | 🔴 1 例红 |

> 经验（同 A 项 §5.4）：**变体锚点必须断言唯一命中**（`hits !== 1` 直接拒绝生成），
> 否则会静悄悄改到别处、拿到假的「守卫生效」。V1b 这类对照组用来证明「红」确实来自被破坏的语义。
> 另：V7 一度**变绿**——新增的「粘性续接」早返回把它掩盖了；按同一规矩（变体绿 = 守卫失效或被人掩盖）
> 把 V7 改成同时破坏两处，才重新成为真守卫。
>
> ⚠️ **单测绿 ≠ 真机可用**：本项 24 例全绿、11 条守卫全红，上 240 仍然**四轮里翻车三轮**
> （前奏无声 / 跳转卡住 / 播一会就停 / 整曲 md5 不符）—— 四次根因**都只在真机形态下成立**
> （首响等数据 / 补块被自己的 signal 掐断 / 续接逐 chunk 回源 / 续接后回头读缓存）。
> ⇒ **性能优化必须留下真机端到端口径**（`patch/probe_B3c_240.js`：真 ffmpeg `-ss` + 数 Range + 看 PCM 字节数，
> `probe_B3d_240.js`：md5 三方比对），单测只能证明「字节对」，证明不了「不卡」。详见 §6.8。

230 验证：`tsc --noEmit` 通过；**全量 vitest 184 文件 / 1563 用例全绿**；9 个 CI 门禁脚本全 0。

**降级开关**：`RAW_STREAM_CACHE=0` 回到旧的纯透传（A/B 对照用）；
另有 `RAW_CACHE_BLOCK_KB` / `RAW_CACHE_SOURCE_MB` / `RAW_CACHE_SOURCES` / `RAW_CACHE_MIRROR_MB`。
回退透传时会打 `[rawStreamCache]` 告警（30s 节流）—— 生产上「播到一半静音」没有日志就只能靠猜。

### 6.8 240 真机实测记录（2026-09-23，共四轮，前三轮均回滚）

**四轮形态与症状**（每轮都在用户反馈后**立即回滚**，再离线定位根因）：

| 轮次 | 当时的形态 | 真机症状 | 根因（回滚后定位） |
|---|---|---|---|
| B3 | 先把 256KB 窗口下载完再构造响应 | 「前面几秒没有声音，拖动后正常」 | 首响应被等数据拖到几十秒 ⇒ §6.6 红线 |
| B3 | 响应即发，但命中时把窗口阶梯重置 | 「跳好几个数字，一直卡住」；整曲回环 41.5s vs 直连 6.5s | 26MB 顺序播放退化成 ~100 次小请求 |
| B3b | 续接=每 chunk 现场开一次上游请求 | 「播正常一会，然后停住」 | 缓存(≈2 秒音频)放完后逐 chunk 往返 ⇒ 21 分钟零输出（§6.6 第 7 条） |
| B4 | 续接交给上游后仍回头读缓存 | 整曲 md5 与直连不符、两次回环互不相同 | 同一段被吐两次，整体错位（§6.6 第 5 条，差异偏移 786432=3×256KB） |
| **B5** | 续接一条请求读到完 + 不再回头读缓存 + 5xx 重试 | — | **全部探针通过 → 用户收听验收通过 ✅** |

**B5 的 240 实测数据**（26MB 天翼网盘 FLAC，`patch/probe_B3_240.js` / `probe_B3c_240.js` / `probe_B3d_240.js`）：

- **整曲回环不截断、不退化**：`len=26080025` 与直连一致；直连读两遍与回环读两遍 **md5 三方全等**；
  `X-MusicFlow-RawCache: hit;upstream=0`；3.5s（直连 3.7s，0.95×）。
- **命中零上游**：命中块 13ms（回源块 292~448ms）。
- **端到端 `ffmpeg -ss`**：冷态 **1.3~1.9s**（旧基线 3.4~8.9s）、同曲热态 **31ms**。
- **日志**：`续接彻底失败` **0** 次；`回退透传` 2 次（正常降级路径）；`续接重试` 若干（网盘偶发 500，重试后成功）。
- **裸 TCP 复验响应头**（`patch/probe_cl_240.js`）：命中分支确实发出 `content-length: 26080025`，
  `content-type` 与上游逐字一致 —— fetch 侧看到的 `cl=null` 是 **探针假象**，不是丢头。

**⚠️ 已知差距：回溯序列没有做到「9 → 1~2」**。实测 5 次回溯（每次往前退 1~2KB）**只命中 1 次、回源 4 次**
（旧版纯透传是 5 次全回源）。原因是补块落地的 TTFB ≈450~500ms，与 ffmpeg 回溯请求的间隔**同一量级**
⇒ 只有最后 1~2 次能等到前缀落盘。**要「第一次回溯就命中」只能等数据 ⇒ 撞 §6.6 红线，不能做**。
所以 B 的收益主要来自 **整曲续读走缓存 + 热态重放**（31ms vs 8862ms），而不是「回溯全命中」——
§4.1 里写的「9 → 1~2」是**设计目标**，真机口径未达成，如实记为已知差距。

### 6.9 交付与交接（2026-09-23 收尾）

**变更集合**（本轮 B 项全部改动，随 `v4.0.13` 发版）：

| 文件 | 性质 |
|---|---|
| `backend/src/services/dlna/rawStreamCache.ts` | **新增** —— 回环 raw 分支的 256KB 稀疏块缓存代理（`proxyRawRange`）|
| `backend/tests/services/rawStreamCache.test.ts` | **新增** —— 24 例，真 HTTP 上游（分片慢给 / hold / 整块 416 / 5xx 注入）|
| `backend/src/routes/rest/index.ts` | 改 —— `/dlna/stream/:token?raw=1` 由纯透传改为 `proxyRawRange` |
| `docs/PLAYBACK_SEEK_OPTIMIZATION.md` | 改 —— §6 全节（形态红线 / 测试矩阵 / 真机记录 / 本节）|

**部署形态（交接要点）**：
- 240 当前跑的是 **B5 热补丁**（`docker cp` 进 `musicflow:/app/backend/dist/...`，容器**重启后仍在**，
  但**拉新镜像会被覆盖**）。正式版的正确形态是 `v4.0.13` 镜像 ⇒ 用户升级步骤见 Release notes。
- **回滚点**：240 `/tmp/rollback-B3/index.js`（B 项之前的 `routes/rest/index.js`）
  + `/tmp/rollback-B/index.js`（更早一轮）。回滚姿势：copy 回容器 + **删掉** `dist/services/dlna/rawStreamCache.js`。
- 关闭开关：`RAW_STREAM_CACHE=0`（回纯透传，用于 A/B 对照）；块大小/预算见 §6.7 末尾。
- **验收条件**：见 **§6.4**（离线 O1~O7 + 真机 R1~R6，共 13 条，均已通过）；每条的取数命令也写在那里，
  下次复验照着跑即可，不必重新设计判据。

**已知差距（不要当已解决）**：

1. **回溯序列未达成「9 → 1~2」**：真机 5 次回溯只命中 1 次。根因是补块 TTFB ≈450ms 与回溯间隔同量级，
   要「首次回溯就命中」只能等数据 ⇒ 撞 §6.6 首响应红线，**明确不做**。真实收益在整曲续读 + 热态重放（31ms）。
2. **前四轮真机问题的共同启示**：单测全绿 ≠ 真机可用。本轮 24 例单测 + 11 条守卫全过，
   上真机仍翻车三轮 —— 三次根因都只在真机形态下成立。**性能改动必须留真机端到端口径**（§6.7 末尾）。

**下一步（未开工项，见 §4.1 总表）**：

- **F 项**（避免重复解析行）与 **A / C / D**（各链路自有优化）**均未开工**；
  B 是共用管道，这五条链路已经先吃到了 B 的收益。
- 可选：`bytes=0-` 曾出现 408 —— 经裸 TCP 复验属探针侧交互假象，如需彻底排除可再做一次独立复验。

## 7. 其它四条链路的同一问题（DLNA / 客户端 / Web / AirPlay）【2026-09-22 调研 · F 项未开工】

> 本节回答用户问题：「服务端推 DLNA、客户端自己播放、Web 播放、AirPlay 播放，跳转进度时能不能也用 A 这个优化？」

> ### ⛔ 本节四条链路**各自的**优化，目前一项都没做
> - **A 没做、也没法照搬**（形态不适用，见 §7.1）；
> - **F 未开工**（§7.3 只是落点设计，代码未动）；
> - **C / D 同样未落地**（它们自己那条链路的改造还没写）。
>
> 但 **B 已落地**（2026-09-23，回环 raw 分支的稀疏块缓存）—— B 是**共用管道**
> （五条链路的 seek 都走 `audio/pipeline.ts` 的 `-ss`），所以 **B 的收益这五条链路一起吃到**，
> 只是各链路**自有的那部分**（A/F/C/D）仍未做。落地状态见 §4.1。

### 7.1 结论：能，但**别照搬 A 的形态**

| 链路 | seek 走法 | 每次重跑的解析 | 是否跑 `resolvePlayableRow` |
|---|---|---|---|
| **Web 播放** | 客户端带 `timeOffset` 重拉 `/rest/stream` | `resolvePreferredSong()`（`routes/rest/index.ts:1686`） | ❌ |
| **客户端自己播放**（Flutter/安卓/Windows） | 同上（**同一条路**） | 同上 | ❌ |
| **服务端推 DLNA** | `reseekByRecast`（`dlna/control.ts:1018`）→ `castToDevice` → 设备重拉 `/rest/dlna/stream/:token` → `serveCastStream` | `resolvePreferredSong()`（`:1881`） | ❌ |
| **AirPlay** | 原地换 decoder 重拉 `/rest/airplay/stream/:token` → 同上 `serveCastStream` | 同上 | ❌ |
| （附）**DLNA 切歌判定** | `QueueController.judgePlayable`（`:735`） | `resolvePlayableRow()` ← **A 原本治的就是它** | ✅ |

**出流入口只有两个**：`/rest/stream` 与 `serveCastStream` —— 两者**都只调 `resolvePreferredSong`，
不调 `resolvePlayableRow`**。⇒ A（`preferRowId`）那套「调用方记账」只对 **sendspin pump** 与 **`judgePlayable`** 成立。

**为什么这四条链路不能照搬 A**：它们是**无状态 HTTP 重拉** —— 浏览器 / 客户端 / 音箱 / 本地 ffmpeg
都不知道「上一轮用的是哪一行」，**没有可记账的调用方**。若硬要按 A 的形态做，得给
`CastSession`（`dlna/control.ts:199`，现只有 `{token, songId, deviceId, createdAt, expiresAt}`）加 rowId、
再给 `/rest/stream` 按 peerId 另建一套记账 —— 改动面远大于收益，**不划算**。

### 7.2 但它们确实在重复付同一笔钱（实测）

`resolvePreferredSong` 的方向 1（web 行 → 组内 local/WebDAV 优选）每次都真去探测候选：

| 事实（240 容器内实测，2026-09-22） | 值 |
|---|---|
| `songs` 总数 | 123324 |
| `type` 分布 | `web` 71080 / `local` 52244 |
| **`path` 前缀分布** | **`web` 71080 / `w` 52244** |
| ⇒ **本地行 100% 是 WebDAV**（`w:` 前缀） | 探测**永远走网络**，命中不了 `existsSync` 快路径 |
| `probeLocalSourceOk`（WebDAV） | **1036 / 1068 / 1181 / 2925 ms** |
| `resolvePlayableRow`（web 行） | **1985 / 2026 / 2248 ms**（`reason=preferred-swap`） |
| 有「web 行 + 组内 WebDAV 兄弟」可优选的歌 | **5665 首** |

**生产铁证**：同一首 `2d57525c` 的 `播放优选:web 歌曲切换到核心曲库源` 在 2 分钟内出现 **9 次**，
其中成对相隔 **1.77~2.10s**（`14:02:30.248`/`14:02:32.119`、`14:03:15.752`/`14:03:17.798`、`14:03:32.549`/`14:03:34.645`）
—— 同一动作触发**两次**解析（judge 一次 + 出流一次），每次都重跑 WebDAV 探测。

**顺带的双重浪费**：一次 web 行 `resolvePlayableRow` 会对**同一个候选文件探测两次** ——
`resolvePreferredSong` 内部的 `probeLocalSourceOk(alt)` 一次、`verifyRow(alt)` 又一次；
而 `utils/localSourceProbe.ts` **只缓存失败（5 分钟）、成功不缓存** ⇒ 两次都是真网络往返。

### 7.3 F 项 · 把「WebDAV 可播」这个结论做成短 TTL 记忆【未开工】

**落点**：`backend/src/utils/localSourceProbe.ts` —— 与现有的 `localFailCache`（失败记忆 5 分钟）对称，
补一个**成功记忆**：

```ts
const localFailCache = new Map<string, number>(); // 现状：只记失败（TTL 5min）
// F 项：给 webdav 分支补一个成功记忆
const webdavOkCache  = new Map<string, number>(); // songId -> 成功时间戳（TTL 同量级）
```

| 要点 | 说明 |
|---|---|
| **只缓存 WebDAV 分支** | 本地 `l:` 路径是 `existsSync`（零成本）→ **缓存它零收益、只引入「文件删了还返回死行」的风险**，因此不缓存 |
| **TTL 与失败记忆对称** | 现状「失败判死」5 分钟内不重试（文件恢复了也仍判死）⇒ 成功记忆取同量级是自洽的 |
| **兜底** | 出流失败（404 / 上游 5xx）时逐出该条；并对外暴露一个 `evictProbeOk(songId)` 供调用方主动清 |
| **零链路改动** | `resolvePreferredSong` 的 **6 个调用点**（`/rest/stream` / `serveCastStream` / `serveFlowQueue` / `ensurePlayableStream` 探测 / `resolvePlayableRow` ×2）**全部自动受益**，且顺带消掉 7.2 的双重探测 |

**预期收益**：这四条链路每次 seek 省 **1.0~2.9s**；叠加 `verifyRow` 的重复探测，实际更多。

**验收**：240 上对同一首走 WebDAV 优选的歌**连拖 3 次**：
- `播放优选:web 歌曲切换到核心曲库源` 日志只在**首轮**出现；
- 后续轮次 TTFB 不再有 ~2s 台阶；
- `Failed to send audio chunk` = 0、设备出声三件套齐全。

### 7.4 与 B 项的关系（两条都要治）

四条链路 seek 也走 ffmpeg 输入定位（见 §6.5）⇒ **B 的收益（4~13s）大于 F（1~3s）**。
但两者**不重叠**：F 治「选源那一步」，B 治「拿到输入之后那一段」。建议 **F + B 一起上**，
顺序上先做 B（收益大、且五条链路通吃），F 紧随其后。

## 8. 各链路验收方式（出问题要能定位到段）

> **原则**：每条链路都要能在**不看 UI、不靠人耳**的前提下先拿到服务端证据，再用**设备侧硬判据**收口
> （用户规矩：修复必须测试过播放正常）。三段开销（①选源 / ②ffmpeg 输入 seek / ③WebDAV 探测）
> 各有独立可观测信号，**别混着看**。

### 8.1 通用验收（五条链路都要过）

| 判据 | 怎么取 | 通过标准 |
|---|---|---|
| **落点正确** | 服务端 `[pump][seek] want=… target=…` / `[DLNA][seek] … 重投流完成 timeOffset=Ns` | 落点 = 目标值；随后位置 **1:1** 推进（2s 采样 +2s） |
| **真的在出声** | 见 §8.2 各链路「出声判据」 | 出声判据齐全 + 抓包零字节占比 **<1%** |
| **无异常路径** | `docker logs musicflow \| grep -cE "放行切歌\|冻结\|stalled\|提前 EOF\|frozen"` | **= 0** |
| **无重复解析**（F 项验收） | `docker logs musicflow \| grep -c "播放优选:web 歌曲切换到核心曲库源"` | 同一首连拖 3 次，**只在首轮出现** |
| **上游请求数**（B 项验收） | ffmpeg `-v debug` 里 `Range: bytes=` 次数 / raw 路由计数 | ~~**9 → 1**~~ ⚠️ **目标未达成**：240 实测 5 次回溯**只命中 1 次、回源 4 次**（旧版 5 次全回源）—— 见 §6.8「已知差距」，别再拿「9 → 1」当验收线 |
| **端到端耗时** | `docker logs -t` 量「开始获取音源 → 音源就绪」 | 对比基线 **5.1/5.1/5.8/9.4/13.1s** |

> ⚠️ 三条硬规矩：
> ① **`docker restart` 会截断 `docker logs`** → 跨版本对比必须**切换前** `docker logs -t > 落盘`；
> ② **在 240 跑 seek 验证脚本前先问用户是否在操作 UI**（用户拖动会往同一 pump 注入 seek，落点观测失效）；
> ③ **别在 240 并发跑多个 ffmpeg 探针**（拖慢 WebDAV → 推流 starving：设备无声但 pos 在走、日志全绿）。

### 8.2 分链路验收

| 链路 | 出声判据（硬） | 落点判据 | 该链路专属观察点 |
|---|---|---|---|
| **Sendspin** | 设备 VERBOSE 日志三件套齐全：`speaker_mixer:369 Starting` + `i2s_audio.speaker:070 Starting` + `Created ring buffer with size 96000 [speaker_task]`；实体 `MediaPlayerState.PLAYING`；`Failed to send audio chunk` = 0 | `[pump][seek] want=N target=N`；之后 `pos` 每 5s 精确 +5s | 抓包 240→246 零字节 **<1%**；WS 帧恒 **4809B**（9B 头 + 4800B 载荷） |
| **DLNA** | 音箱实际出声 —— **这一条只能人耳**；服务端 `[cast] <deviceId>: BEGIN songId=…` 后不得连续 `Stop failed` 重试 | `[DLNA][seek] <deviceId> SOAP Seek 不可靠 → 重投流重建(timeOffset=Ns)` 之后有 `重投流完成 timeOffset=Ns Xms`；`positionEstimates` 锚到 target | **`unreliableSeek` 设备**才走重投；SOAP 可用的设备应看到 `Seek(REL_TIME)` 成功且落位校验通过 |
| **Web 播放** | 浏览器播放器从目标位置**继续**出声（不是从头） | F12 Network：`/rest/stream?id=X&timeOffset=N` 的 N = 目标秒；看响应**首个音频字节**延迟 | 服务端 `[stream] <id> timeOffset=Ns(拖动重拉)peerId=…` 必须出现（证明拖动真到了服务端） |
| **客户端自己播放**（Flutter/安卓/Win） | 客户端 UI 进度条与声音对齐（**同 Web 一条路**） | 同上（同 `/rest/stream`） | 客户端会带 `peerId`（`local:<uuid>`）→ 可据此区分是哪台设备在拉 |
| **AirPlay** | 设备出声（人耳）；同 host 的 DLNA 会话不被 `stopAirPlaySessionsForHost` 误停 | 原地 seek（RTSP 会话不动、只换 decoder）时**位置不回 0**；回落重投时同 DLNA | `[airplay]` seek 分支日志：原地 vs `seekSec` 重投；`RaopPlayer.realtimeStats` 可观测节拍 |

> **为什么 DLNA / AirPlay 的「出声」只能人耳**：这两条链路的音箱是**独立成品设备**，本仓没有它的日志通道
> （不像 ESP32 可由 ESPHome Native API 订阅 VERBOSE）。可用间接证据是**抓包看到设备持续拉流 + 零字节占比低**，
> 但那**不能证明喇叭在响**（设备可能拉流但不放）。⇒ 这两条链路**首次验收必须人耳确认一次**，
> 之后回归可只跑服务端 + 抓包信号。

### 8.3 按「三段开销」定位故障（出问题时先归段）

| 症状 | 落在哪一段 | 先看什么 |
|---|---|---|
| 位置发布了、但音频迟迟不来 | ①选源 / ②输入 seek | `[resolve] … (reason) ms=` 与 `-ss` 那段耗时 |
| 同一首连拖每次都慢一样多（~2s 台阶） | ③WebDAV 探测（F 未做） | `播放优选:web 歌曲切换到核心曲库源` 是否每次都打 |
| `-ss` 越大越慢、上游请求多 | ②输入 seek（B 未做） | `ffmpeg -v debug` 的 `Range: bytes=N-` 次数 |
| 设备无声但服务端 pos 在走、日志全绿 | **推流 starving**（不是 seek 问题） | 是否在 240 并发跑了 ffmpeg 探针 |
| 拖完进度条自己跳回去 | `SEEK_SETTLE_MS=8s` 未生效 | `player/seekSettle.ts` 的 `idle_early` 拦截日志 |

## 9. C / D / E 项（未开工 / 已撤回）

- **C · 窗口内 seek 走零成本路径**：`PcmWindow.seekTo()` 已实现「命中窗口 → 只挪消费水位、不重起 ffmpeg」
  （`backend/src/services/sendspin/streamSource.ts:234` 的快速分支），但 `seekCore` 目前**总走完整起播重建**
  ⇒ 每次拖动都杀一条 ffmpeg 再拉一条。要让落点落在「已解且未淘汰」区间内时走零成本分支。
  ⚠️ 必须先核对与 `1937f80`（起播窗口内 seek 的世代竞态）的语义边界，**不能把那批修复重新引回来**。
- **D · 前端拖拽预热**：拖动过程中节流预取目标附近字节，把 B 的首个 miss 提前消化。
  卡片 `_seek()` 与网页 `castSeek()` 现状是 **250ms trailing 去抖**（客户端只发 1 次），改前先理清节律。
- **E · `-noaccurate_seek`**：孤立 ffmpeg 实测省 29%（请求数不变），真实路径无法确认收益，
  已按用户决定摘除；原文见 §11.1。

## 10. 附注

### 10.1 诊断脚本（本机 `opencode230/patch/`）

`probe_webdav.js`（源站 Range/TTFB）、`probe_chain.js`（链路逐步计时）、`probe_af.js`（af 链对照）、
`probe_loop.js`（回环 vs 直连）、`probe_http.js`（ffmpeg debug 抓 Range 序列）、`probe_perreq.js`
（单请求耗时分离）、`probe_local.js`（本地文件对照）、`probe_E_240.mjs`、
**`probe_B_240.js`（B 项三条对照，本轮新增）**、
**`probe_pref_cost2.mjs`（§7.2 四条链路调研，2026-09-22 新增）**。

### 10.2 三个坑（已并入 `MEMORY.md`）

1. 给 ffmpeg 传 `-headers` 必须用**数组传参**（`execFileSync(FF, [args...])`）—— 走 shell 会把 `\r\n` 截断，
   ffmpeg 直接 `Error opening input`，量出来的耗时全是假的。
2. 临时 raw token 走 **INSERT 进 `raw_stream_tokens`**（注册表在 SQLite，跨进程可见），别用内存 Map。
3. **在 240 测「模块耗时」用独立进程直接 import dist**：
   `docker exec -w /app/backend musicflow node /tmp/x.mjs`。
   ⚠️ 但**抽样口径要看 `pluginEntry` 而不是 `type`** —— `resolvePlayableRow` 用
   `row.pluginEntry` 判「是不是 web 行」，`resolvePreferredSong` 用 `song.type === "web"`，
   两者口径不同；首版抽样按 `type` 取，恰好全落在 no-swap 样本上，**误判「优选免费」**（已修正）。

### 10.3 相关文档

- `docs/PLAYBACK_SEEK_MA_REWORK.md` —— seek 的语义 / 正确性改造（patch1~10、§6.6 世代竞态）
- `SPEC.md` §1.7 / §1.8 —— `preferRowId` 契约、ffmpeg 输入硬契约
- `docs/audio-pipeline-plan.md` §1.3 / §3.6 —— 转码管道与通道能力表
- `docs/SENDSPIN_ESPHOME_DEBUG.md` §4.1 —— 设备侧「出声三件套」验收判据

## 11. 附录

### 11.1 2026-09-22 原文备查 · E 项（`-noaccurate_seek`）

> 逐字保留被撤回的方案原文与实测，以免以后重走弯路。

```
E `-noaccurate_seek`（实测省 29%，请求数不变）

services/audio/pipeline.ts：DecodeRequest.coarseSeek → 在 -ss 之前追加 -noaccurate_seek
  （输入选项，必须在 -i 前，否则 ffmpeg 当输出选项静默无效）。
services/sendspin/streamSource.ts：推流 decodeArgs({...}) 传 coarseSeek: true。

撤回原因：孤立 ffmpeg 上量到 -29%，但真实 seek 重建路径上无法确认收益，
且同期出现的取流超时经取证另有根因（见 docs/PLAYBACK_SEEK_MA_REWORK.md §6.6）。
```

### 11.2 修订记录

| 日期 | 修订 |
|---|---|
| 2026-09-22 | 建立本文（原名「Sendspin 跳转进度优化专项」）。固化 ①A（已完成 `1bcd473`，含完整优化方法与负向矩阵）+ ②B/C/D/E 五项候选；本轮决定实施 B。E 项原文备查见 §10.1。 |
| 2026-09-22 | **改名《所有播放链路跳转进度优化方案》**：范围从 sendspin 扩到全部五条链路。新增 §7（DLNA / 客户端 / Web / AirPlay 的同一问题调研 + **F 项**方案、§7.2 实测数字与生产铁证）、§6.5（B 覆盖五条链路）、§10.2 第 3 条（独立进程量模块耗时的抽样口径坑）；候选清单由 A~E 扩为 **A~F**。 |
| 2026-09-22 | 新增 **§8 各链路验收方式**（通用验收矩阵 + 五条链路分项判据 + 按三段开销定位故障）；其后章节下移（原 §8/§9/§10 → §9/§10/§11）。 |
| 2026-09-22 | **明确落地范围**：新增 §4.1「落地状态总表（按链路 × 优化项）」，并在 §0、§7 顶部标注 —— **除 Sendspin 外，其余四条链路的优化一项都还没做**（A 形态不适用、F/B/C/D 均未落地）。此前 §7 只是「能 + 怎么做」的结论，容易被误读为已做。 |
| 2026-09-22 | **B 项收益实测入档**：§6.3 补「回环透传 4708/8862/3369ms vs 本地文件 13/13/10ms（≈680×，收益上界）」；新增 **§6.3.1 主方案原型验证**（`patch/probe_C3_240.js`：热缓存 **23~72ms**、上游请求 **0**；冷态 3875ms 且根因是 4MB 块过大——实测回溯序列集中在目标点前后 ~85KB）；§6.2 块大小改为 **256KB 量级 + seek 前目标点预热**，并记下原型三次踩坑（无界响应死循环 / `spawnSync`+同进程 HTTP 服务事件循环死锁 / 块大小拍脑袋）。 |
| 2026-09-23 | **B 项代码落地**：新增 `services/dlna/rawStreamCache.ts`，`routes/rest/index.ts` 的 raw 分支由纯透传改为 `proxyRawRange`。§6.2 重写为**实际落地形态**并列出与原型方案的**三处出入**（缓存改进程内内存、不做时间→字节预热、不自造有界响应）；新增 **§6.6 形态红线**（首响应不等数据 —— 两轮 240 真机回归的共同根因，附「后台补块别带 signal」「短读必须续抓」「后台 fetch 必须 catch」「空洞不能合并」四条真机形态坑）与 **§6.7 测试/负向矩阵**（20 例 + 8 变体，V1b 对照组全绿）。状态统一标为 **✅ 已落地 · ⏳ 240 真机实测待做**。 |
| 2026-09-23 | **B 项四轮 240 实测 + 收口**：§6.6 补第 5~7 条真机形态坑（续接后不得回头读缓存 / 5xx 不得静默收尾 / 续接必须一条请求读到完——21 分钟零输出事故）；**新增 §6.8 240 真机实测记录**（四轮形态表 + B5 全部数据 + **已知差距：回溯序列实测 5 次只命中 1 次，未达成「9 → 1~2」目标**，并说明这是 TTFB 与回溯间隔同量级所致、不能靠等数据解决）；§6.7 换成**权威矩阵 12 变体 / 11 条守卫各自精确变红 + V1b 对照组 24 全绿**，并给「回溯只回源 2 次」标注**单测口径**；§7 顶部改为「B 已落地（共用管道 ⇒ 五条链路一起吃到），A/C/D/F 各自仍未做」。状态标为 **✅ 已落地 · 🔬 240 已部署（B5）· ⏳ 待收听验收**。 |
| 2026-09-23 | **B 项验收通过 + 交接**：用户在 240 上收听验收通过（起播/跳转后立刻有声、跳转后不再卡住）。§0/§4.1/§6 标题/§6.8 状态统一改为 **✅ 已落地并验收**；**§6.4 由三条笼统 bullet 改写为「验收条件与结果」两段式表 —— 离线 O1~O7 + 真机 R1~R6 共 13 条，每条给出取数命令与通过标准**（并把「回溯 9 → 1~2」明确列入「不列为验收条件/不做」）；**新增 §6.9 交付与交接**（变更集合 / 部署形态与回滚点 / 验收条件交叉引用 / 已知差距 / 下一步未开工项）。随 `v4.0.13` 发版。 |
