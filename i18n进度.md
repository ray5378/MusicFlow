# i18n 国际化进度

> 本仓库（主仓库：服务端 + 前端 Web）的 i18n 整体任务需求与当前进度。语言方向：**中文默认 + English**，全局 `--gate` 由 CI 守卫强制。

## 整体任务需求

- **前端**：vue-i18n 基座（默认中文 `zh-CN`）。设置页作为标准参考迁移，其余视图按批次迁移：`admin` → `主布局/首页/音乐` → `歌单/收藏` → `专辑/歌手/流程`。

- **后端**：响应层错误统一迁入中英 catalog（`errors.*`，含模板参数插值），共约 140 键。

- **插件机制**：`PluginManifest` 可选 `i18n` 字段（`{ zh?, en? }` 子字典，**默认中文、zh 可省略只须补 en**），覆盖 `name / description / platformLabels / groups / configSchema` 的 `label / help / options`（见 `docs/PLUGIN_DEV.md` §3.4）。字段级本地化经 `pluginI18n.ts`（`resolvePluginI18n / resolveField / localName / localDesc`）渲染。

- **CI 守卫**（均在 `backend/scripts/`）：

  - `check-i18n.mjs`：键对齐（140 zh = 140 en）、残留扫描；

  - `check-core.mts`：核心代码平台名耦合黑名单 + 计数阈值；

  - `check-frontend-plugins.mjs`：前端插件隔离正向保障（弹窗渲染 manifest 声明内容）。

## 当前进度：已完成 ✅（随 v2.0.0 发版）

| 阶段                         | 说明                                                        | 提交           |
| -------------------------- | --------------------------------------------------------- | ------------ |
| 前端 i18n 基座 + 设置页参考迁移       | vue-i18n、默认中文、`t()` 体系、设置页全量                              | `d67c4ad`    |
| 后端错误 catalog + 插件侧 i18n 字典 | `errors.*` 140 键、`PluginManifest.i18n` 机制、`pluginI18n.ts` | `a7756ab`    |
| CI 守卫修复                    | frontend lockfile 补 `vue-i18n`（npm ci/许可门禁）               | `212472e`    |
| CI 构建修复                    | 歌单卡片辅助函数、Flows 循环变量遮蔽 `t`、CoverPlay 默认文案                  | `4f8bef4`    |
| 守卫误报修复                     | en 文案去除平台名黑名单、插件弹窗守卫适配 `configFields` 派生结构                | `afaf238`    |
| 发版                         | 误导 tag 清理 + `v2.0.0` 重大更新发布                               | tag `v2.0.0` |

## 待办 / 后续

- 深度 `throw new Error("中文")`（`group/flows/playerWebhook` 等）需 **catch 级翻译**后再回填 catalog（当前仅内部错误残留）。

- ~~各内置插件 manifest 的~~ ~~`i18n.en`~~ ~~文案批量补齐（当前仅~~ ~~`playPreference.ts`~~ ~~提供 en 示例）~~ ✅ 已完成：13 个内置插件全部补齐 `i18n.en`（`artistInfo` / `dailyRecommend` / `dailyRoam` / `localPlatformRecommend` / `localRecommend` / `playlistSync` / `randomSongs` / `core/songGroup` / `importers.qq` / `importers.netease` / `importers.native` / `renderers.dlna` / `renderers.airplay`），覆盖 `name / description / platformLabels` 与 `configSchema` 各字段及注入的 `scheduleEnabled / runOnBoot / batchParallel` 开关文案；默认中文保证向后兼容。

## 关联仓库

- 客户端全量迁移见 `MusicFlow-client/i18n进度.md`（进行中）。

- 插件清单 i18n 见 `MusicFlow-plugins/i18n进度.md`。

- 联动仓库批次见 `hass-musicflow` / `hass-musicflow-card` / `hassio-addons` 各自 `i18n进度.md`。

